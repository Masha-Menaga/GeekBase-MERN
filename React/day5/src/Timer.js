import React from 'react'

const Timer = () => {
    return (
        <div>
            <button>Start Timer</button>
            <button>Stop Timer</button>
        </div>
    )
}

export default Timer